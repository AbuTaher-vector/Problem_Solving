#include <bits/stdc++.h>
 
using namespace std;
 
int main() {
 
    int x;
    int mx=INT_MIN;
    int pos;
    for(int i=1;i<=100;i++)
    {
        cin>>x;
        if(x>mx)
        {
            mx=x;
            pos=i;
        }
    }
    cout<<mx<<'\n'<<pos<<'\n';
 
    return 0;
}
