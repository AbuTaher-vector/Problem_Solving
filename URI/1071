#include <iostream>
 
using namespace std;
 
int main() {
 
    int a,b,sum=0;
   cin>>a>>b;
   for(int i=min(a,b)+1;i<max(a,b);i++)
   {
       if(abs(i)%2==1) sum+=i;
   }
   cout<<sum<<'\n';
 
    return 0;
}
