#include<bits/stdc++.h>
 
using namespace std;
 
int main() {
 
    vector<int>v;
    int x,t=10;
    while(t--)
    {
        cin>>x;
        if(x<=0) v.push_back(1);
        else v.push_back(x);
    }
    for(int i=0;i<10;i++) cout<<"X["<<i<<"] = "<<v[i]<<'\n';
    
 
    return 0;
}
