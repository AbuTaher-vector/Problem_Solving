#include <iostream>
 
using namespace std;
 
int main() {
 
    int x;
    while(1)
    {
        cin>>x;
        if(x==0) break;
        int sum=0;
        if(x&1) x++;
        for(int i=x;i<=x+8;i+=2) sum+=i;
        cout<<sum<<'\n';
    }
 
    return 0;
}
