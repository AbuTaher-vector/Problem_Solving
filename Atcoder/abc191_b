#include<bits/stdc++.h>
using namespace std;
#define nn int
#define dd double
#define ll long long int
#define ss string
int main()
{
   nn a,b;
   cin>>a>>b;
   vector<nn>v;
   while(a--)
   {
       nn x;
       cin>>x;
       if(x!=b) v.push_back(x);
   }
   for(nn i=0;i<v.size();i++) cout<<v[i]<<" ";
   cout<<endl;
 
}
