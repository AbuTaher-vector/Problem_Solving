#include<bits/stdc++.h>
using namespace std;
void code(){
  int n;
  cin>>n;
  int x;
  set<int>s;
  for(int i=0;i<n;i++){
      cin>>x;
      s.insert(x);
  }
  if(s.size()==2){
      cout<<n/2+1<<"\n";
  }else cout<<n<<"\n";

}
int main(){
  int tt;
  cin>>tt;
  while(tt--) code();
}
