class Solution {
public:
    int distributeCandies(vector<int>&v) {
        
        set<int>s;
     for(int i=0;i<v.size();i++) s.insert(v[i]);
     int x=min(s.size(),v.size()/2);
        return x;
        
    }
};
