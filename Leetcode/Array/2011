class Solution {
public:
    int finalValueAfterOperations(vector<string>&v) {
        int ans=0;
     for(int i=0;i<v.size();i++)
     {
          if(v[i].back()=='+' || v[i].front()=='+') ans++;
          else ans--;
     }
     return ans;
        
    }
};
