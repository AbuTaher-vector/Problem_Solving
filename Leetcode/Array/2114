class Solution {
public:
    int mostWordsFound(vector<string>&v) {
        int mx=0;
     for(int i=0;i<v.size();i++)
     {
          int cnt=0;
          for(int j=0;j<v[i].size();j++) if(v[i][j]==' ') cnt++;

            cnt++;
         mx=max(mx,cnt);
     }
     return mx;
    }
};
