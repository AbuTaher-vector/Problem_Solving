class Solution {
public:
    char findTheDifference(string s, string t) {
         char xr=' ';
      for(int i=0;i<s.size();i++) xr^=s[i];
      for(int i=0;i<t.size();i++) xr^=t[i];
      xr=tolower(xr); 
      return xr; 
        
    }
};
