class Solution {
public:
    long long int mySqrt(int x) {
        long long int lo=0,hi=x;
	long long int mid;
	while(lo<hi)
	{
		mid=(hi+lo)/2;
		if(mid*mid<x) lo=mid+1;
		else hi=mid;

	}
	if(lo*lo==x) return lo;
	else if(hi*hi==x) return hi;

	return lo-1;
    }

};
