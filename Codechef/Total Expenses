#include<bits/stdc++.h>
using namespace std;
#define dd double
int main()
{
    int tt;
    cin>>tt;
    while(tt--)
    {
        dd q,p,ans;
         scanf("%lf %lf",&q,&p);
        if(q>1000) ans=(p*q-(p*q)/10);
        else ans=(p*q);
        printf("%lf\n",ans);
    }
}
