#include<bits/stdc++.h>
using namespace std;
int main()
{
    int tt;
    cin>>tt;
    while(tt--)
    {
        int x;
        cin>>x;
        int arr[x+1];
        for(int i=0;i<x;i++) cin>>arr[i];
        int a=arr[0];
        for(int i=1;i<x;i++) a=__gcd(a,arr[i]);
        for(int i=0;i<x;i++) cout<<arr[i]/a<<" ";
        cout<<endl;

    }
}
