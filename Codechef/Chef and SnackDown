#include<bits/stdc++.h>
using namespace std;
#define nn int
#define dd double
#define ll long long int
#define ss string
int main()
{
      nn tt;
      cin>>tt;
      while(tt--)
      {
          nn y;
          cin>>y;
          if(y==2010 || y==2015 || y==2016 || y==2017 || y==2019) cout<<"HOSTED"<<endl;
          else cout<<"NOT HOSTED"<<endl;
      }


}
