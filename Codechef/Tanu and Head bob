#include<bits/stdc++.h>
using namespace std;
#define dd double
int main()
{
    int tt;
    cin>>tt;
    while(tt--)
    {
        int x,k=0,l=0;
        cin>>x;
        string s;
        cin>>s;
        for(int i=0;i<s.size();i++)
        {
            if(s[i]=='I')
            {
                k=1;
                break;

            }
            else if(s[i]=='Y')
            {
                 l=1;
                break;
            }
        }
        if(k==1) cout<<"INDIAN"<<endl;
        else if(l==1) cout<<"NOT INDIAN"<<endl;
        else if(k==0 && l==0) cout<<"NOT SURE"<<endl;

            }
}
