#include<bits/stdc++.h>
using namespace std;
#define ll long long int
int main()
{
    ll tt;
    cin>>tt;
    while(tt--)
    {
          ll x,fact=1;
          cin>>x;
          for(ll i=x;i>1;i--) fact*=i;
          cout<<fact<<endl;

    }
}
