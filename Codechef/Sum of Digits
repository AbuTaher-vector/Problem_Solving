#include<bits/stdc++.h>
using namespace std;
int main()
{
    int tt;
    cin>>tt;
    while(tt--)
    {
        int x,sum=0;
        cin>>x;
        while(x!=0)
        {
            sum+=x%10;
            x/=10;
        }
        cout<<sum<<endl;
    }
}
