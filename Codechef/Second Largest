#include<bits/stdc++.h>
using namespace std;
int main()
{
    int tt,arr[4];
    cin>>tt;
    while(tt--)
    {
        for(int i=0;i<3;i++) cin>>arr[i];
        sort(arr,arr+3);
        cout<<arr[1]<<endl;
    }
}
